## 오늘 한 것

- pull request 연습
- 프로젝트 step-1 플로우차트 그려보기

## Closure

- 코드의 블럭
- Swift에서 일급객체로써 전달인자, 변수 ,상수 등으로 저장,전달이 가능하다.
- 매개변수, 반환 타입, return키워드, in 을 생략 할 수 있다.
- 함수는 이름 있는 클로저이다.

```swift
//closure
{ (parameter) -> returnType in
statements
}
```

```swift
let names = ["Chris", "Alex", "Ewa", "Barry", "Daniella"]

func backward(_ s1: String, _ s2: String) -> Bool {
		return s1 > s2
}
var reversedNames = names.sorted(by: backward)
// ["Ewa", "Daniella", "Chris", "Barry"]
```

```swift
//위의 코드를 축약하여 다음과 같이 바꿀 수 있다.
let names = ["Chris", "Alex", "Ewa", "Barry", "Daniella"]

var reversedNames = names.sorted { $0 > $1 }
//후행클로저
```
